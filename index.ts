import makeFile from './src/makeFile';
import makeString from './src/makeString';

const moduleExports = {
    makeFile,
    makeString
};

export default moduleExports;
export { makeFile, makeString };